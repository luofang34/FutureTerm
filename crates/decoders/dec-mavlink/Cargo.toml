[package]
name = "dec-mavlink"
version = "0.1.0"
edition = "2021"

[dependencies]
core-types = { path = "../../core-types" }
mavlink = { version = "0.12", default-features = false, features = ["common", "serde", "std"] }
web-sys = { version = "0.3", features = ["console"] }
